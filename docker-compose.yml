version: '3.8'

services:
  postgres_fox:
    container_name: postgres_fox
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=pg_fox